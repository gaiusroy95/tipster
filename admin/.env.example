# Local dev — backend API (includes /api prefix)
VITE_API_URL=http://localhost:3000/api

# Production (Vercel): set VITE_API_URL=/api so requests use admin/vercel.json proxy rewrites.
# SPA routes (/login, /users, …) are handled by the same vercel.json fallback to index.html.